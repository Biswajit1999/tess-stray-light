# React/Tailwind research page

This is a local starter, not a source of scientific truth. The demo chart uses `public/project.json`. The build pipeline must replace demo content with outputs generated by the Python pipeline and preserve the provenance/limitations panels.

```bash
npm install
npm run dev
npm run build
```
